/*for (var r = 0; r < rows; r++) {
	for (var c = 0; c < cols; c++) {
		if (patterns[r][c] == null) {
			continue;
		}
		
		patterns[r][c].draw(x + 32 * c, y + 32 * r);
	}
}

if (x < 800) {
	draw_set_font(fntStageH_Hue);
	draw_set_color(c_white);
	draw_text_outline(x, y + 340, "Find the two similar 2x2", c_black);
}*/